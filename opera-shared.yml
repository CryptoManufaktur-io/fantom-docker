# To be used in conjunction with erigon.yml, nethermind.yml, besu.yml or geth.yml
services:
  opera:
    ports:
      - ${HOST_IP:-}${RPC_PORT}:${RPC_PORT:-8545}/tcp
      - ${HOST_IP:-}${WS_PORT}:${WS_PORT:-8546}/tcp
